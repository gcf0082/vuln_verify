# 认证鉴权缺失

**适用**：被校验的主张是某模块存在认证鉴权缺失，但该模块只是整个系统中的子模块/子服务。

- **默认非漏洞**：子模块通常由统一认证鉴权组件（如网关、入口服务、Spring Security 配置、IAM 等）负责鉴权，默认不认定为认证鉴权缺失。
- **是漏洞**：该子模块作为完整服务端独立运行，且能直接观察到其自身入口存在明显鉴权缺失（如控制器方法无注解、无拦截器、无 Token 校验等），攻击者可绕过统一鉴权直接访问。
- **无法确认**：无法判断该模块是否独立运行、统一鉴权是否覆盖当前接口，或调用链上其他模块的鉴权逻辑不可见。
