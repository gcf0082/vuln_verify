# 本地提权

**适用**：被校验的主张是低权限用户提权。

核心：低权限用户能否构造脚本、配置文件或输入，被高权限进程执行或操作。

- **确认（是漏洞）**：高权限进程执行的命令名或脚本路径可被低权限用户控制--利用 `$PATH` 搜索顺序即可触发非预期执行，不必控制 `$PATH` 中的文件。
- **证伪（非漏洞）**：命令名/路径不可被低权限用户控制，或高权限进程不执行用户可控内容。
