# 环境变量取默认值

**适用**：被校验的主张与获取环境变量并取默认值有关（如 `System.getenv("X") ?: "default"`、`getenv(x, default)`）。

获取环境变量时使用默认值，通常只是配置回退，不直接构成安全或合规问题：

- **默认非漏洞**：默认值导致的配置结果，一般不认为是问题。
- **是漏洞**：有证据证明默认值本身危险（如默认开启调试接口、默认硬编码敏感凭据、默认值可被攻击者通过其他路径覆盖并触发危害），且危害触发条件现实成立。
- **无法确认**：环境变量/默认值的实际来源与可控性存在不可补缺口。
