# /tmp 目录提权

**适用**：被校验的主张是读写 `/tmp` 下文件/子目录导致的提权。

- **确认（是漏洞）**：目标路径为 `/tmp/<子目录>/` 且子目录名不含随机数（如 `/tmp/dir1/`）--攻击者可预先创建并建立符号链接劫持程序读写。
- **证伪（非漏洞）**：子目录名含随机数（如 `/tmp/upload_1a2b3c/`），路径不可预测（结合上下文判断）。
