---
name: vuln-verify
description: "白盒代码漏洞与合规风险校验。输入一个或多个漏洞/合规/隐私风险（任意格式），验证它们是不是真的。多个时按上下文智能分发子 agent：默认一漏洞一 agent，仅当共享校验上下文（同攻击面/Sink/路径/根因变体）才合并为一组，各组独立走 复述->举证->判断。方法：独立重建判定--先假设漏洞成立，再沿判定要素逐条举证证伪（输入驱动型找全防护、追可控源、验路径可达；非输入型查危险行为、触发条件、补偿），基于证据强度裁定。裁定为 是漏洞 / 非漏洞 / 无法确认。当用户要求'验证这个漏洞是不是真的'、'这些漏洞存在吗'、'这个漏洞存在吗'、'校验一下这个漏洞'、'这个SAST告警能利用吗'、'帮我确认这个漏洞'、'构造PoC'时，即使未使用正式安全术语，也应触发本 skill。不适用于：无具体漏洞的开放性挖掘、纯代码质量审查。"
---

# vuln-verify：白盒漏洞与合规风险校验方法论

> 本方法论回答四个问题：判什么（三种裁定）、什么时候用（适用与不适用）、怎么判（形态要素 -> 三步流程 -> 校验原则）、结果怎么交付（报告与回复）。

## 判定目标与三种裁定

**验证漏洞，就是检查它成立的要素是不是都满足。**

漏洞主张说"这里有危险"。验证就是先把危险成立需要的条件都找出来--数据从哪来、路上经过什么处理和防护、危险操作在什么条件下触发、有没有别的机制兜底--然后看这些条件能不能同时成立：

- **是漏洞**：所有要素都成立--输入驱动型能构造出穿透防护的 PoC，非输入型能触发实际危害（有前提条件的，列出条件）
- **非漏洞**：至少一个要素被证伪--防护有效阻断、Source 不可控、路径不可达，或危险行为不存在/不触发/被补偿
- **无法确认**：信息不足，既构造不出 PoC 也证明不了不可能

不靠直觉，不靠经验，靠证据。确认不了就如实说确认不了，不硬下结论。

---

## 适用与不适用

- **输入**：一个或多个漏洞/合规/隐私风险（任意格式描述）
- **环境**：可访问的源代码（白盒分析）
- **不适用**：无具体漏洞/合规/隐私风险主张的开放性挖掘、纯黑盒测试

---

## 多漏洞的分组与分发

输入可能是一个或多个漏洞。

- **单个漏洞**：直接进入下方校验流程。
- **多个漏洞**：先按"是否共享校验上下文"分组，再为每组分发一个子 agent 并行执行，最后汇总。

**默认一漏洞一 agent。** 仅当共享校验上下文时才合并到同一 agent：

- 同一攻击面/接口、同一 Sink 或危险函数、同一数据流路径（共享 Source/Path），或同一根因/同一漏洞类型的不同参数与变体--合并可复用同一段路径分析，避免重复追溯。

**不合并**：不同漏洞类型、不相关代码区域、不同形态（输入驱动型 vs 非输入型）的，强行合并只会撑大上下文、并让一份结论被另一份带偏。

**分组要保守**：宁可多分一个 agent，也不把松散相关的塞在一起。一个 agent 内若含多个漏洞，仍须**逐个独立走完 复述->举证->判断，分别给出裁定**，不得合并结论。汇总时按漏洞逐条列出裁定。

分发给子 agent 的 prompt 必须携带落盘要求（见「报告与回复」一节的「报告落盘」）：保存目录、命名规则、分配给该组的漏洞序号、完整报告结构。子 agent 每校验完一个漏洞即自行写盘，返回时只需回传每个漏洞的裁定结论与报告文件路径，不回传完整报告。

---

## 两类形态与判定要素

漏洞按成因分两类形态，判定要素不同。**对应形态的要素缺一，漏洞即不成立。**

> **合规与隐私风险同样纳入校验范围**：过度收集个人信息、日志/响应中未脱敏的敏感数据、未按政策启用加密/匿名化、弱/过期加密算法等合规/隐私问题，也按本流程校验并落盘。这类风险通常归入「非输入型」，按「危险行为存在、触发条件现实成立、无有效补偿」三要素判定；**裁定时统一按漏洞处理**，结论仅三种：是漏洞 / 非漏洞 / 无法确认，不再单独区分「合规问题」「隐私风险」。

### 输入驱动型（注入、路径穿越、SSRF、文件上传等）

三个要素**同时**成立：

1. **可控源（Source）**：攻击者可控的数据从哪里进入。不同入口可控范围不同--HTTP 参数完全可控，文件/环境变量通常不可控，数据库/消息队列数据可能二次注入（视为未知），外部系统返回值默认不可信。
2. **可达路径（Path）**：数据能从 Source 一路流到 Sink，PoC 在中间不被拦住。路径上每一跳都是一个条件：

   | 条件类型 | 含义 | 示例 |
   |---|---|---|
   | 变换条件 | 数据经处理后，输入要满足什么才能在变换后仍触发 | `substring(0,10)` 后超长 PoC 被截断 |
   | 防护条件 | 校验/转义/参数化收紧了允许的输入范围 | 白名单阻断、正则校验、参数化绑定 |
   | 运行时条件 | 到达此路径所需的前提，不满足则不可达 | 鉴权状态、配置开关、数据状态 |

3. **失效防护（Sink）**：危险操作处没有防护，或者防护有真能走通的绕过办法。关键是搞清楚什么样的输入到了这里能触发漏洞--同一行代码不同参数触发条件可能完全不同（如 `exec(String)` 走 shell 可注入，`exec(String[])` 不走则不可注入）。

路径上的防护分两种，分析重点不同：
- **门控型**：检查后决定放不放行（if 判断、正则、白名单）。重点：检查逻辑能不能绕过。
- **变换型**：转换数据内容（转义函数、编码、参数化）。重点：转换有没有覆盖当前 Sink 的注入字符--对 XSS 有效的转义对 SQL 注入可能完全无效。

### 非输入型（硬编码凭据、弱加密、配置缺陷、敏感信息日志、内存安全、逻辑缺陷等）

三个要素**同时**成立：

1. **危险行为存在**：代码中确实存在该危险行为，有原文证据（如硬编码了密钥、使用了弱算法）。
2. **触发条件现实成立**：该行为在系统真实运行中会被触发，危害落到真实资产上--不是测试桩、占位值、未启用路径或臆想场景，有代码事实支撑。
3. **无有效补偿**：不存在使危害不成立的补偿机制（如凭据已被强制修改、证书是公开信息、危险操作前置了校验）。

---

## 校验流程：复述 -> 举证 -> 判断

### 第 1 步：复述漏洞

用自己的话复述这个漏洞：**什么类型、在哪个位置、造成什么影响**，并归到上述某类形态。复述不清说明理解不到位，先追问澄清。引用了 CVE 或补丁的，分析 diff 可以加速后续步骤。

原始漏洞描述如果带了 PoC，那只是参考--PoC 不对不代表漏洞不存在。任务是验证漏洞是否存在，不是验证原始 PoC 能不能工作。

复述之后，把漏洞成立所需的条件拆成不超过 5 条的**判定清单**，后续步骤围绕清单展开。例如主张"某接口存在命令注入"，清单可以是：① 输入来自外部且攻击者可控；② 输入原样拼进了 shell 命令；③ 路径上没有有效过滤；④ 现有校验可被绕过。第 2 步举证就是逐条核对这些条件，第 3 步裁定对每条给出 成立 / 不成立 / 无法确认。

### 第 2 步：举证无漏洞

**独立重建**：不照单接受漏洞主张里的任何断言（包括"无校验""校验失效""防护有效"）。主张依赖的每个关键事实，主动找到对应代码自行核实；核实范围覆盖整个数据流（上下游调用链、各层中间件、注解式校验、辅助方法），不只核对主张提到的点。

**按漏洞类型加载专项规则**：根据漏洞类型与描述的场景，查 `references/vuln-rules-index.md`，命中就只加载该类型对应的那一个规则文件（禁止全量加载，也不加载不匹配的），把其确认/证伪要点纳入下面的举证。例如主张命令注入加载 `command-injection.md`，主张敏感信息泄露加载 `sensitive-info-leak.md`。规则文件给出该类型下的确认（是漏洞）与证伪（非漏洞）要点，用来加强举证，不替代判定方法论--最终裁定仍按判定要素与证据强度给出。

举证 = 站在"无漏洞"立场，**先构造最强攻击假设，再沿判定要素逐条尝试证伪**：

**先定性再判断**：判断输入可不可控、触发条件现不现实之前，先给代码和数据定性：① 这段代码是什么性质--生产代码、对外库接口，还是示例/测试/第三方内嵌代码（示例、测试类的默认立场见 `references/false-positives.md`）；② 数据是谁给的--外部用户、其他租户、可信运维，还是本地配置文件。定完性，再判断 Source 可不可控、触发条件现不现实。

**输入驱动型：**
1. **找全防护**：沿 Source 到 Sink 逐跳找出所有校验、转义、参数化、白名单。每个防护贴出代码原文和 file:line--**包括注解式校验**（`@NotNull`、`@Size`、`@Pattern`、`@Valid` 等）。声称"校验存在绕过"时，必须贴出校验代码本身，否则不构成证据。
2. **检查 Source 可控性**：Source 必须追溯到真正的外部入口，**尽可能向上找到数据的最初来源，禁止止步于单个文件**。被分析对象是一个脚本（如 Python 脚本）时，脚本本身通常只是中间环节--必须找到它的调用方（谁启动/调用了这个脚本：其他代码的 `subprocess`/`os.system` 调用、Web 接口、定时任务、CI 配置等），把调用方传入的参数、stdin、环境变量、输入文件一并纳入分析，并沿调用链继续向上追溯。若找到的 Source 是脚本 `main()`、库函数参数、内部服务处理函数等中间入口，同样不能停下--继续追溯谁调用它、传入什么数据，直到外部调用方，由它决定可控性。调用方就在当前仓库却未追溯的，算举证没做完，不得直接下"输入不可控"的结论。按来源类型设定默认立场--外部系统数据默认不可信，数据库/消息队列数据视为未知（可能二次注入），文件/环境变量默认不可控（除非有证据证明可被外部控制）。
3. **检查路径可达性**：从 Source 到 Sink 有没有可达路径。路径断开则贴出断点代码。追踪链断在三方库边界时，记下断点位置和最后追到的函数。

**非输入型：**
1. **危险行为是否存在**：贴出原文证据，确认主张的危险行为真实存在。
2. **触发条件是否现实成立**：追溯该行为是否在真实运行路径上被触发、危害是否落到真实资产，排除测试/占位/未启用场景。
3. **是否有补偿**：搜索使危害不成立的补偿控制（强制修改、前置校验、公开信息等）。

**解释 PoC 走不通**：针对原始报告或自己推导的 PoC，逐跳解释它在哪一步被阻断、被什么防护拦住（非输入型则说明触发条件为何不成立或被补偿）。

**证据要求与证据强度：**
- 每条主张必须贴出 file:line + 代码原文（**直接证据**）。
- 由命名、上下文、框架惯例推断而未读到原文的为**间接证据**；信息缺失无法推断的为**未知**。**结论强度不得超过证据链的最弱一环。**
- 声称"无防护"时，必须主动搜遍整个流程的所有校验点--注解式校验、中间件、辅助方法、上下游调用链，确认确实没有或都已失效。**未发现防护 ≠ 无防护。**
- **信息缺口可补时必须先补再判**（函数定义就在当前仓库、尚未读取），不得直接落"无法确认"；只有缺口不可补（三方库黑盒、源码不可得）才落"无法确认"，并写清缺什么、缺口在哪个要素、拿到什么证据才能下结论。

**特例**：输入驱动型风险的输入源仅能追溯到数据库、消息队列等存储时，因数据可能由其他模块的外部用户写入，源可控性视为"未知"，按"无法确认"处理，不得定为"非漏洞"。

### 第 3 步：判断

基于举证结果给出裁定。

> **裁决前必查 `references/false-positives.md`**：其中的场景有默认判断，举证未推翻默认时按默认裁定。

- **非漏洞**：至少一个要素被**直接证据**证伪--防护有效且不可绕过、Source 不可控、路径不可达，或危险行为不存在/不触发/被补偿。说明每个证伪为什么成立。仅凭间接推断"应该安全"不得定非漏洞。
- **是漏洞**：全部要素均有直接证据（或大部分直接、其余强间接且证伪未发现反证，此时须注明最弱证据环及其位置）。必须给出完整的利用或危害触发方式--输入驱动型给出攻击步骤与关键 PoC 证明能触发所描述的问题，非输入型证明危险行为/不合规操作在真实运行中被触发并造成危害。原始描述的利用方式走不通就换方向重新构造。
- **无法确认**：证伪中止于不可补的信息缺口。必须说明：哪部分信息缺失、最后能确认到哪一步、需要补充什么才能确认。

**漏洞类型基于事实**：具体的漏洞类型必须有代码级事实支撑--例如参数未校验直接写入数据库，不能因此认定为 XSS，必须有前端渲染且未转义的证据才能认定 XSS。若举证发现真实问题与主张不同（如主张 XSS、实际是 SQL 注入），按实际发现裁定并说明差异。

**基于当前代码，不假设未来变更**：漏洞永远基于当前代码状态。禁止以"未来代码可能变更导致漏洞"为由认定或升级漏洞。

---

## 校验原则

1. **双向质疑**：说"有漏洞"要证明危险可被实际利用（输入驱动型：PoC 能穿透全部校验点到达 sink；非输入型：触发条件现实成立且无补偿），说"无漏洞"要证明要素中至少一个被有效阻断。两个方向都要**同强度的直接证据**，不靠"看起来危险"或"看起来安全"下结论。
2. **利用即证据**：仅凭"存在危险代码/配置"不构成漏洞。输入驱动型必须有可工作的 PoC（逻辑类漏洞用等价替代证明，例如证明某条业务逻辑可被绕过而不触发预期检查）；非输入型必须证明危险行为在真实运行中被触发且造成实际危害。
3. **证据链**：每个结论都能追溯到具体代码位置（file:line + 原文）。不允许逻辑跳跃，推理的每一步都要有代码证据。质疑本身也要有代码事实支撑，不能为了推翻结论而编造不存在的假设或攻击场景。
4. **不漏条件**：检查有没有漏掉条件。一个结论出错，往往不是因为推理错了，而是因为有一层条件没被纳入分析。
5. **独立重建**：验证的是主张是否成立，不是复述主张。主张里的断言要当作待核实的假设，逐条用代码验证，不只核对主张提到的点。
6. **诚实标注缺口**：不回避不确定。可补的缺口先补，不可补的缺口如实标出位置和所需证据，不强行下结论。

---

## 报告与回复

### 报告落盘

每个漏洞的分析报告**必须落盘保存，一漏洞一文件**，内容为完整的详细分析过程，禁止只写结论摘要。

- **保存位置**：当前工作目录下的 `vuln-reports/` 目录，不存在则创建。
- **命名规则**：`NN-<类型>-<位置简写>.md`。`NN` 为本次输入漏洞的两位序号（01、02…，由主 agent 统一分配、随派发传给子 agent）；类型如 `sql-injection`、`xss`、`hardcoded-credential`、`privacy-data-leak`、`compliance-missing-encryption`；位置简写取 Sink 或危险操作所在的类/方法，如 `UserService-query`。同名冲突时追加 `-2`、`-3`。示例：`vuln-reports/03-command-injection-ExecUtil-run.md`。
- **写入时机**：每个漏洞的 复述->举证->判断 走完后**立即写盘**；多 agent 并行时由各自子 agent 自己写，而不是汇总阶段由主 agent 统一代写--保证报告是校验现场产生的完整分析过程，不被二次压缩。
- **文件内容**：按本节「报告模板」写全所有字段（裁定、主张摘要、逐跳证据链、Source 可控性/触发条件、防护清单、关键代码片段、裁定依据、利用方式/信息缺口、修复建议），`file:line` + 代码原文逐跳展开，不以自然语言概括代替代码。一个子 agent 校验了多个漏洞时，也要逐个分别写文件，不合并。

### 报告模板

以下模板是**写入报告文件**的模板（见本节「报告落盘」）。报告要自包含、可追溯--原始描述可能很简略，报告要把缺的信息补全。每个结论都附 `file:line` + 代码原文，证据链逐跳展开，不以自然语言概括代替代码。

```markdown
## 裁定: {是漏洞 | 非漏洞 | 无法确认}

## 漏洞主张摘要
<规范化的主张：什么类型、在哪个位置（file:line）、造成什么影响、归入哪类形态。原始描述简略时在此补全为完整画像>

## 证据链（从入口到危险操作，逐跳展开）
<输入驱动型：从外部 Source 到 Sink，逐跳列出每段代码、它对数据做了什么、PoC 在这一跳还能不能通过>
  - {file:line} `{代码片段}` -- {这一跳是什么、做了什么：数据入口 / 加工或校验 / 危险操作 / 造成的后果，PoC 在这是否被拦}
  - ...
<非输入型：从入口到危险操作的调用链，逐跳列出>
  - {file:line} `{代码片段}` -- {这一跳是什么、做了什么}
  - ...

### Source 可控性 / 触发条件
<输入驱动型：Source 追溯到真正的外部入口，给出 可控 / 不可控 / 未知 及依据>
<非输入型：危险行为在真实运行中是否被触发、危害是否落到真实资产>

### 防护清单
<逐一列出找到的防护（含注解式校验 @NotNull/@Size/@Pattern/@Valid 等），每条附 file:line + 原文，标注：有效阻断 / 可绕过（如何绕过）/ 已失效；并说明正确的防护应该是什么样（如"对 SQL 注入，参数化绑定才算有效防护，这里的 HTML 转义不构成防护"）>

## 关键代码片段
<贴出对裁定起决定作用的原文：Source 入口、Sink、关键防护、断点位置，每段标注 file:line>

## 裁定依据
<基于证据强度为什么得出此裁定；逐要素说明证伪成功或失败，指出最弱证据环>
<若与原始主张不同，指出主张哪里判断有误、哪个代码证据导致结论不同>
<验证依据：结论基于静态代码追踪；PoC 是推导出来的、没实际跑过（如实际跑过，写明跑了什么、看到什么）--没跑过的 PoC 不能写得像跑过一样>

## 利用方式（仅 是漏洞）
<输入驱动型：完整攻击步骤 + 关键 PoC，说明它如何逐跳穿透防护触发所描述的漏洞>
<非输入型：危险行为如何被触发并造成危害>
<有前提条件的，列出条件>

## 信息缺口（仅 无法确认）
<缺失什么信息、缺口在哪个要素、已追踪到哪一步、需要补充什么才能下结论>

## 修复建议（可选，谨慎给出）
<针对确认的漏洞或可疑点的修复方向>
```

### 聊天回复

校验全部完成后，聊天回复只放一张汇总表，不粘贴完整报告：每行一个漏洞，给出序号、漏洞、裁定、一句话理由、报告文件路径。多数条目裁定相同（如大量"无法确认"）时，在表后补一句共性原因说明。

```markdown
## 校验汇总

| # | 漏洞 | 裁定 | 理由 | 报告 |
|---|------|------|------|------|
| 01 | UserService.query SQL 注入 | 是漏洞 | 参数未过滤直达拼接 SQL | vuln-reports/01-sql-injection-UserService-query.md |
| 02 | ExecUtil.run 命令注入 | 非漏洞 | 数组执行且元素全部硬编码 | vuln-reports/02-command-injection-ExecUtil-run.md |
```
