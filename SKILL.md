---
name: vuln-verify
description: "白盒代码漏洞校验。输入一个漏洞（任意格式、单个），验证它是不是真的。方法：举证代码没有漏洞——沿 Source 到 Sink 逐跳找全所有防护，检查 Source 可控性和路径可达性，基于举证结果判断漏洞是否存在。裁定为 是漏洞 / 非漏洞 / 无法确认。当用户要求'验证这个漏洞是不是真的'、'这个漏洞存在吗'、'校验一下这个漏洞'、'这个SAST告警能利用吗'、'帮我确认这个漏洞'、'构造PoC'时，即使未使用正式安全术语，也应触发本 skill。不适用于：无具体漏洞的开放性挖掘、纯代码质量审查。"
---

# vuln-verify：白盒漏洞校验方法论

## 核心命题

**漏洞验证 = 约束满足问题。**

漏洞主张声称"攻击者输入能到达危险操作"。验证就是把这条路径上的所有条件收集起来——输入从哪来、经过了什么处理、有什么防护、需要什么前提——然后判断这些条件是否让攻击成为不可能：

- **是漏洞**：防护有缺口，能构造出穿透的 Payload（有前提条件的，列出条件）
- **非漏洞**：证明所有路径都被有效防护阻断，不可能构造出来
- **无法确认**：信息不足，既构造不出 Payload 也证明不了不可能

不靠直觉，不靠经验，靠证据。确认不了就如实说确认不了，不硬下结论。

---

## 约束系统的三要素

### Sink（终点）
危险操作在哪里。关键是搞清楚：什么样的输入到了这里能触发漏洞。同一行代码不同参数的触发条件可能完全不同（例如 `exec(String)` 走 shell 可注入，`exec(String[])` 不走则不可注入）。

### Source（起点）
攻击者可控的数据从哪里进入程序。不同入口的可控范围不同：HTTP 参数完全可控，文件/环境变量通常不可控，数据库数据可能二次注入，外部系统返回值默认不可信。

### Path（路径）
Source 到 Sink 之间的数据流。路径上每一跳都是一个条件——输入必须满足什么才能穿透这一跳：

| 条件类型 | 含义 | 示例 |
|---|---|---|
| **变换条件** | 数据经过处理后，输入要满足什么才能在变换后仍能触发 | `substring(0,10)` 后超过 10 字符的 payload 被截断 |
| **防护条件** | 校验/转义/参数化收紧了允许的输入范围 | 白名单阻断、正则校验、参数化绑定 |
| **运行时条件** | 到达此路径所需的前提，不满足则路径不可达 | 鉴权状态、配置开关、数据状态 |

路径上的防护分两种，分析方法不同：
- **门控型**：检查后决定放不放行（if 判断、正则、白名单）。分析重点：检查逻辑能不能绕过。
- **变换型**：转换数据内容（转义函数、编码、参数化）。分析重点：转换有没有覆盖当前 Sink 的注入字符——对 XSS 有效的转义对 SQL 注入可能完全无效。

---

## 校验流程（3 步）

```
复述漏洞 → 举证无漏洞 → 判断
```

### 第 1 步：复述漏洞

用自己的话复述这个漏洞：**什么类型、在哪个位置、造成什么影响**。复述不清说明理解不到位，先追问澄清。引用了 CVE 或补丁的，分析 diff 可以加速后续步骤。

原始漏洞描述如果带了 payload，那只是参考——payload 不对不代表漏洞不存在。任务是验证漏洞是否存在，不是验证原始 payload 能不能工作。

### 第 2 步：举证无漏洞

尝试证明代码没有漏洞。主动找出所有能证明"漏洞不存在"的证据：

1. **找全防护**：沿 Source 到 Sink 的路径，逐跳找出所有校验、转义、参数化、白名单等防护。每个防护贴出代码原文和 file:line。

2. **检查 Source 可控性**：找到 Source 的实际来源，判断攻击者能否控制。按来源类型设定默认立场——外部系统数据默认不可信，数据库数据视为疑似（可能二次注入），文件/环境变量默认不可控（除非有证据证明可被外部控制）。

3. **检查路径可达性**：从 Source 到 Sink 有没有可达路径。如果路径断开，贴出断点代码。

4. **解释 payload 走不通**：针对原始报告或自己推导的 payload，逐跳解释它在哪一步被阻断、被什么防护拦住。

**证据要求：**
- 每条主张必须贴出 file:line + 代码原文
- 声称"无防护"时，必须主动搜遍整个流程的所有校验点——注解式校验、中间件、辅助方法、上下游调用链，确认确实没有或都已失效
- 未发现防护 ≠ 无防护

### 第 3 步：判断

基于举证结果给出裁定：

- **非漏洞**：举证完整，所有路径都有有效防护，Source 不可控或路径不可达。说明每个防护为什么有效。
- **是漏洞**：举证发现缺口——防护缺失且 Source 可控且路径可达。构造 Payload 证明缺口可利用。原始 payload 不对就换方向重新构造。
- **无法确认**：举证不完整——信息不足，既无法证明所有路径都被防护，也无法确认缺口可利用。必须说明：哪部分信息缺失、最后能确认到哪一步、需要补充什么才能确认。

---

## 校验原则（4 条）

1. **双向质疑**：说"有漏洞"要证明 Payload 可构造，说"无漏洞"要证明防护确实覆盖了所有路径。两个方向都要证据，不靠"看起来危险"或"看起来安全"下结论。
2. **Payload 即证据**：没有可工作的 Payload 就不算是漏洞（逻辑类漏洞需要等价替代证明——例如证明某条业务逻辑可被绕过而不触发预期检查）。
3. **证据链**：每个结论都能追溯到具体代码位置（file:line + 原文）。不允许逻辑跳跃，推理的每一步都要有代码证据。质疑本身也要有代码事实支撑，不能为了推翻结论而编造不存在的假设或攻击场景。
4. **约束完备性**：检查有没有漏掉条件。一个结论出错，往往不是因为推理错了，而是因为有一层条件没被纳入分析。

---

## 输出结构

```markdown
## 裁定: {是漏洞 | 非漏洞 | 无法确认}

## 漏洞主张摘要
<规范化的假设：什么类型、在哪个位置、造成什么影响>

## 举证记录
<沿 Source → Sink 逐跳的防护分析，每条附 file:line + 代码原文>
<Source 可控性判定>
<路径可达性判定>

## 裁定依据
<基于举证结果为什么得出此裁定>
<如果是漏洞：构造的 Payload 及它如何穿透缺口>
<如果是无法确认：缺失什么信息、需要补充什么>
```

## 使用前提

- **输入**：一个漏洞（任意格式描述，单个漏洞）
- **环境**：可访问的源代码（白盒分析）
- **不适用**：无具体漏洞的开放性挖掘、纯黑盒测试、批量验证（每次只处理一个漏洞）
