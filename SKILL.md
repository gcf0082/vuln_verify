---
name: vuln-verify
description: "白盒代码漏洞校验。输入一份漏洞报告（任意格式、单个漏洞），验证该漏洞是否真实存在。核心方法论：将漏洞验证视为约束满足问题——从报告主张出发，识别约束系统（Sink/Source/Path），沿路径收集约束后反向求解构造Payload，再自我对抗地证伪该Payload，最终给出 TRUE POSITIVE / FALSE POSITIVE / CONDITIONAL 裁定。当用户要求'验证这个漏洞是不是真的'、'这个报告说的漏洞存在吗'、'校验一下这个漏洞'、'这个SAST告警能利用吗'、'帮我确认这个漏洞'、'构造PoC'时，即使未使用正式安全术语，也应触发本 skill。不适用于：无报告的开放性漏洞挖掘、纯代码质量审查。"
---

# vuln-verify：白盒漏洞校验方法论

## 核心命题

**漏洞验证 = 约束满足问题。**

一份漏洞报告声称"存在一条从攻击者输入到危险操作的可利用通路"，验证就是把这个主张形式化为一个约束系统，然后求解：

- **TRUE POSITIVE** ＝ 找到一个满足所有约束的具体输入（Payload）
- **FALSE POSITIVE** ＝ 证明约束空间为空，不存在任何满足所有约束的输入
- **CONDITIONAL** ＝ 仅在特定条件集合下约束可满足

校验的成败不取决于直觉或经验，而取决于能否构造出（或证明不能构造出）一个满足全部约束的输入。

---

## 约束系统的三要素

每个漏洞验证面对的约束系统由三个要素构成，验证的第一步是识别它们：

### Sink（约束终点）
危险操作所在的代码位置。回答"什么输入形态在此处能触发漏洞"——这是约束系统的**目标条件**。同一行代码的不同参数、不同调用方式，目标条件可能完全不同（例如字符串形态的 exec 与数组形态的 exec 有不同的触发条件）。

### Source（约束起点）
攻击者可控数据首次进入程序的入口。回答"攻击者能输入什么"——这是约束系统的**输入空间**。入口不同（HTTP 参数/文件/环境变量/数据库/消息队列/第三方 API 返回值），输入空间的边界不同。

### Path（约束传播）
Source 到 Sink 之间的数据流路径。路径上的每一跳都是约束：

| 约束类型 | 含义 | 示例 |
|---|---|---|
| **变换约束** | 数据经过处理后，输入必须满足什么形态才能在变换后仍保持触发能力 | 经过 `substring(0,10)` 后，超过 10 字符的 payload 被截断 |
| **防护约束** | 校验/转义/参数化收紧了允许的输入空间 | 白名单阻断、正则校验、参数化绑定 |
| **运行时约束** | 到达此路径所需的前提条件，不满足则路径不可达 | 鉴权状态、配置开关、数据状态 |

路径上的防护按分析方式不同分为两种：
- **门控型**：检查后决定是否放行（`if` 判断、正则校验、白名单）。分析重点是判断逻辑是否能被绕过。
- **变换型**：转换数据内容（转义函数、编码器、参数化绑定）。分析重点是转换是否覆盖了当前 Sink 的注入向量——对 Sink A 有效的变换可能对 Sink B 完全无效。

---

## 校验流程（6 步）

```
 理解主张 → 定位约束系统 → 收集约束 → 求解约束 → 证伪解 → 裁定
```

每一步都有明确的 FP 出口，不必走完全程就能给出裁定。

### 第 1 步：理解主张

把任意格式的漏洞报告（自然语言描述、SAST 输出、CVE、JSON、Markdown）解析为一个规范的待验证假设：**声称什么漏洞、在哪个位置、造成什么影响**。

如果报告过于模糊无法提取这些信息 → 追问澄清。如果报告引用了 CVE 或补丁，分析 diff 可以加速后续步骤。

### 第 2 步：定位约束系统

识别并核验约束系统的三个要素：

1. **核验 Sink**：找到报告声称的位置，确认危险操作是否存在。精确识别注入面——同一函数不同参数的注入风险不同。**如果 Sink 不存在、位置错误或代码不可达 → FALSE POSITIVE（错误位置）。**

2. **识别 Source**：不信任报告声称的 Source，找到攻击者可控数据实际从哪里进入。标注可控程度和跨越的信任边界。**如果没有外部可控的数据源 → FALSE POSITIVE（不可达）。**

3. **追踪 Path**：建立 Source 到 Sink 的数据流，每跳标注变换类型（污点保持/污点去除/门控）。注意路径可能有分支（if-else、try-catch、多赋值），每一条分支独立分析。**如果从 Source 到 Sink 不存在任何可达路径 → FALSE POSITIVE（不可达）。**

### 第 3 步：收集约束

沿路径逐跳收集三类约束，形成约束清单：

- **变换约束**：输入经过此变换后，输出还能触发 Sink 吗？如果能，输入必须满足什么条件？
- **防护约束**：此防护阻断了哪些输入？能否绕过？Gate 型防护检查逻辑是否有漏洞，Transform 型防护检查覆盖的字符集是否匹配当前 Sink 的注入向量。
- **运行时约束**：攻击者能否满足到达此路径的前提条件？

### 第 4 步：求解约束

**从 Sink 反向推导**：Sink 需要什么输入形态 → 逐层反推每步变换之前需要什么 → 到达 Source 得到"攻击者必须输入什么"。

这是整个方法论的核心推理动作。它把"从 Source 向 Sink 猜测 payload 会触发什么"转换为"从 Sink 的目标条件出发，沿着变换链反向计算如何构造输入"。约束不可满足（反向推导到某一步发现了矛盾 → 无法构造出满足所有约束的输入）→ 可能是 FALSE POSITIVE。

如果约束可满足，构造出具体的 Payload。

### 第 5 步：证伪解

对自己的解做**自我对抗式审查**——这是区分该任务与"纯发现漏洞"的核心步骤：

- **正向模拟**：Payload 从 Source 开始，逐步走过每个变换，是否真的以触发形态到达 Sink？
- **约束完备性**：是否遗漏了约束？常见的遗漏来源包括：编码/字符集/截断、框架级防护（中间件/安全头/模板引擎）、运行时前提（鉴权/配置/数据依赖）、隐式路径。
- **假设验证**：构造过程中是否做了假设（如"这个函数是透传的"）？每个假设都需要验证。

如果 Payload 未通过证伪 → 修复后重新证伪，或降级裁定为 CONDITIONAL / FALSE POSITIVE。

### 第 6 步：裁定

综合约束求解与证伪结果，给出最终判定：

- **TRUE POSITIVE**：存在满足所有约束的 Payload 且通过证伪。先说影响再说技术细节。
- **FALSE POSITIVE**：约束空间为空，证明不存在这样的 Payload。说明不可满足的约束具体是什么、在哪一步。
- **CONDITIONAL**：仅当特定条件满足时才存在解。显式列出所有前置条件。
- 附加信息：置信度（High / Medium / Low，基于证据完整性和假设数量）、假设清单（每个假设都是"如果错了裁定会变"的风险点）、证据链摘要。

---

## 校验原则（5 条）

1. **举证倒置**：默认质疑。不假设"这个看起来危险"或"这个看起来安全"，每个结论都需要证据。证明它真而非证明它假。
2. **Payload 即证据**：没有可工作的 Payload 就不算 TRUE POSITIVE（逻辑类漏洞需要等价替代证明——例如证明某条业务逻辑可以被绕过而不触发预期检查）。
3. **自我对抗**：构造 Payload 后必须主动试图打破它。如果自己都不能证伪自己的解，这个解才真正成立。
4. **证据链**：每个结论可追溯到具体代码位置（file:line + 原文）。不允许逻辑跳跃——推理的每一步都 grounded 在代码证据上。
5. **约束完备性**：证伪的本质是检查是否遗漏了约束。一个解失败往往不是因为推理错误，而是因为有一层约束没被纳入分析。

---

## 输出结构

```markdown
## 裁定: {TRUE POSITIVE | FALSE POSITIVE | CONDITIONAL}

## 漏洞主张摘要
<规范化的假设>

## 约束系统
### Sink
### Source
### Path

## 约束清单
<逐跳约束>

## Payload / 不可满足证明
<构造的 Payload 或 约束不可满足的理由>

## 证伪结果
<正向模拟 + 约束完备性检查结果>

## 裁定依据
<为什么得出此裁定 + 置信度 + 假设清单>
```

---

## 与其他 skill 的关系

本 skill 是自包含的通用方法论，不依赖也不替代其他安全审计 skill。其他 skill（如 sink-vuln-confirm、fp-check）处理的是具体场景的特定步骤，本 skill 提供的是适用于所有场景的统一思维框架。

当验证过程中需要回溯源数据流或分析特定防御机制时，本 skill 的方法论自然引导到正确的分析方向，无需外部的检查清单。

---

## 使用前提

- **输入**：一份漏洞报告（任意格式，单个漏洞）
- **环境**：可访问的源代码（白盒分析）
- **不适用的场景**：
  - 无报告的开放性漏洞挖掘（应使用 vuln-hunter 或其他发现类 skill）
  - 纯黑盒测试（无源代码可分析）
  - 批量验证多个漏洞（每次只能处理一个；多个漏洞需逐一提交）

---

## 何时加载本 skill

只要你面临的任务是"验证一份具体的漏洞报告是否真实"——无论报告来自 SAST 工具、人工审计、CVE 描述还是三方通报——都应使用本 skill。本 skill 不处理漏洞发现，只处理漏洞验证。
