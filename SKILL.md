---
name: vuln-verify
description: "白盒代码漏洞校验。输入一个漏洞（任意格式、单个），验证它是不是真的。方法：三方对抗——开发第一责任人举证没有漏洞，渗透测试专家挑战证据尝试绕过，裁判核验双方证据后裁定。裁定为 是漏洞 / 非漏洞 / 无法确认。当用户要求'验证这个漏洞是不是真的'、'这个漏洞存在吗'、'校验一下这个漏洞'、'这个SAST告警能利用吗'、'帮我确认这个漏洞'、'构造PoC'时，即使未使用正式安全术语，也应触发本 skill。不适用于：无具体漏洞的开放性挖掘、纯代码质量审查。"
---

# vuln-verify：白盒漏洞校验方法论

## 核心命题

**漏洞验证 = 约束满足问题。**

漏洞主张声称"攻击者输入能到达危险操作"。验证就是把这条路径上的所有条件收集起来——输入从哪来、经过了什么处理、有什么防护、需要什么前提——然后尝试构造一个能穿透所有条件的输入：

- **是漏洞**：构造出了能穿透所有条件的 Payload（有前提条件的，列出条件）
- **非漏洞**：证明不可能构造出来
- **无法确认**：信息不足，既构造不出 Payload 也证明不了不可能

不靠直觉，不靠经验，靠能否构造出（或证明不能构造出）一个满足全部条件的输入。确认不了就如实说确认不了，不硬下结论。

---

## 三方对抗机制

漏洞验证不是一个人的独角戏，是三方对抗：

| 角色 | 目标 | 何时加载 |
|---|---|---|
| 开发第一责任人 | 举证代码没有漏洞 | 第 2 步加载 `references/developer.md` |
| 资深渗透测试专家 | 挑战开发的证据，尝试绕过 | 第 3 步加载 `references/pentester.md` |
| 裁判 | 核验双方证据是否真实，给出裁定 | 第 5 步加载 `references/judge.md` |

三方共用约束系统（Sink/Source/Path）作为分析框架，但立场相反：开发找"约束不可满足"的证据，渗透找"约束可满足"的证据，裁判验证双方的证据是否真实。

---

## 约束系统的三要素

### Sink（终点）
危险操作在哪里。关键是搞清楚：什么样的输入到了这里能触发漏洞。同一行代码不同参数的触发条件可能完全不同（例如 `exec(String)` 走 shell 可注入，`exec(String[])` 不走则不可注入）。

### Source（起点）
攻击者可控的数据从哪里进入程序。不同入口的可控范围不同：HTTP 参数完全可控，文件/环境变量通常不可控，数据库数据可能二次注入，外部系统返回值默认不可信。

### Path（路径）
Source 到 Sink 之间的数据流。路径上每一跳都是一个条件——输入必须满足什么才能穿透这一跳：

| 条件类型 | 含义 | 示例 |
|---|---|---|
| **变换条件** | 数据经过处理后，输入要满足什么才能在变换后仍能触发 | `substring(0,10)` 后超过 10 字符的 payload 被截断 |
| **防护条件** | 校验/转义/参数化收紧了允许的输入范围 | 白名单阻断、正则校验、参数化绑定 |
| **运行时条件** | 到达此路径所需的前提，不满足则路径不可达 | 鉴权状态、配置开关、数据状态 |

路径上的防护分两种，分析方法不同：
- **门控型**：检查后决定放不放行（if 判断、正则、白名单）。分析重点：检查逻辑能不能绕过。
- **变换型**：转换数据内容（转义函数、编码、参数化）。分析重点：转换有没有覆盖当前 Sink 的注入字符——对 XSS 有效的转义对 SQL 注入可能完全无效。

---

## 校验流程（6 步）

```
复述漏洞 → 开发举证 → 渗透挑战 → 对抗回合 → 裁判核验 → 裁判裁定
```

### 第 1 步：复述漏洞（三方共识）

用自己的话复述这个漏洞：**什么类型、在哪个位置、造成什么影响**。复述不清说明理解不到位，先追问澄清。引用了 CVE 或补丁的，分析 diff 可以加速后续步骤。

原始漏洞描述如果带了 payload，那只是参考——payload 不对不代表漏洞不存在。任务是验证漏洞是否存在，不是验证原始 payload 能不能工作。

### 第 2 步：开发举证（证明没有漏洞）

加载 `references/developer.md`，执行开发第一责任人角色。

开发主动找出所有能证明"漏洞不存在"的证据：路径上的所有防护、Source 不可控的证据、路径不可达的证据、为什么 payload 走不通。

### 第 3 步：渗透挑战（尝试绕过）

加载 `references/pentester.md`，执行资深渗透测试专家角色。

渗透拿到开发的证据，逐条挑战：每个防护能不能绕过、有没有遗漏路径、能不能构造穿透 payload。

### 第 4 步：对抗回合

开发回应渗透的挑战，渗透再回应开发。多轮交替，直到双方都提不出新证据。

### 第 5 步：裁判核验

加载 `references/judge.md`，执行裁判角色。

裁判不重新分析代码，而是逐条核验双方提供的证据：去 file:line 确认代码确实存在且意思与声称一致。

### 第 6 步：裁判裁定

裁判基于核验后的证据给出最终裁定：是漏洞 / 非漏洞 / 无法确认。

---

## 校验原则（5 条）

1. **双向质疑**：说"有漏洞"要证明 Payload 可构造，说"无漏洞"要证明防护确实覆盖了所有路径。两个方向都要证据，不靠"看起来危险"或"看起来安全"下结论。
2. **Payload 即证据**：没有可工作的 Payload 就不算是漏洞（逻辑类漏洞需要等价替代证明——例如证明某条业务逻辑可被绕过而不触发预期检查）。
3. **对抗须充分**：开发和渗透必须都竭尽全力——开发不能只查一处就下结论，渗透不能只试一个方向就放弃。对抗不充分则裁判应判无法确认。
4. **证据链**：每个结论都能追溯到具体代码位置（file:line + 原文）。不允许逻辑跳跃，推理的每一步都要有代码证据。质疑本身也要有代码事实支撑，不能为了推翻结论而编造不存在的假设或攻击场景。
5. **约束完备性**：证伪的本质是检查有没有漏掉条件。一个解失败，往往不是因为推理错了，而是因为有一层条件没被纳入分析。

---

## 输出结构

```markdown
## 裁定: {是漏洞 | 非漏洞 | 无法确认}

## 漏洞主张摘要
<规范化的假设>

## 开发举证
<开发提供的"无漏洞"证据，每条附 file:line>

## 渗透挑战
<渗透提供的绕过证据和 payload，每条附 file:line>

## 对抗回合
<双方交锋记录，最终各自剩余的未驳倒证据>

## 裁判核验
<逐条证据核验结果：成立/不成立/无法验证>

## 裁定依据
<为什么得出此裁定 + 置信度 + 假设清单>
```

## 使用前提

- **输入**：一个漏洞（任意格式描述，单个漏洞）
- **环境**：可访问的源代码（白盒分析）
- **不适用**：无具体漏洞的开放性挖掘、纯黑盒测试、批量验证（每次只处理一个漏洞）
- **角色执行**：支持 subagent 时三个角色各用独立 subagent；不支持时单 agent 依次切换角色，每次切换时读取对应的角色文件
