---
name: vuln-verify
description: "白盒代码漏洞校验。输入一个漏洞（任意格式、单个），验证它是不是真的。方法：找到 Sink（危险操作）和 Source（攻击者输入），追清两者之间的数据流和所有防护，从 Sink 反向推导构造 Payload，再自己试图打破它。裁定为 TRUE POSITIVE / FALSE POSITIVE / CONDITIONAL。当用户要求'验证这个漏洞是不是真的'、'这个漏洞存在吗'、'校验一下这个漏洞'、'这个SAST告警能利用吗'、'帮我确认这个漏洞'、'构造PoC'时，即使未使用正式安全术语，也应触发本 skill。不适用于：无具体漏洞的开放性挖掘、纯代码质量审查。"
---

# vuln-verify：白盒漏洞校验方法论

## 核心命题

**漏洞验证 = 约束满足问题。**

漏洞主张声称"攻击者输入能到达危险操作"。验证就是把这条路径上的所有条件收集起来——输入从哪来、经过了什么处理、有什么防护、需要什么前提——然后尝试构造一个能穿透所有条件的输入：

- **TRUE POSITIVE**：构造出了这样的 Payload
- **FALSE POSITIVE**：证明不可能构造出来
- **CONDITIONAL**：只有满足特定条件时才能构造出来

不靠直觉，不靠经验，靠能否构造出（或证明不能构造出）一个满足全部条件的输入。

---

## 约束系统的三要素

### Sink（终点）
危险操作在哪里。关键是搞清楚：什么样的输入到了这里能触发漏洞。同一行代码不同参数的触发条件可能完全不同（例如 `exec(String)` 走 shell 可注入，`exec(String[])` 不走则不可注入）。

### Source（起点）
攻击者可控的数据从哪里进入程序。不同入口的可控范围不同：HTTP 参数完全可控，文件/环境变量通常不可控，数据库数据可能二次注入，外部系统返回值默认不可信。

### Path（路径）
Source 到 Sink 之间的数据流。路径上每一跳都是一个条件——输入必须满足什么才能穿透这一跳：

| 条件类型 | 含义 | 示例 |
|---|---|---|
| **变换条件** | 数据经过处理后，输入要满足什么才能在变换后仍能触发 | `substring(0,10)` 后超过 10 字符的 payload 被截断 |
| **防护条件** | 校验/转义/参数化收紧了允许的输入范围 | 白名单阻断、正则校验、参数化绑定 |
| **运行时条件** | 到达此路径所需的前提，不满足则路径不可达 | 鉴权状态、配置开关、数据状态 |

路径上的防护分两种，分析方法不同：
- **门控型**：检查后决定放不放行（if 判断、正则、白名单）。分析重点：检查逻辑能不能绕过。
- **变换型**：转换数据内容（转义函数、编码、参数化）。分析重点：转换有没有覆盖当前 Sink 的注入字符——对 XSS 有效的转义对 SQL 注入可能完全无效。

---

## 校验流程（6 步）

```
理解主张 → 定位约束系统 → 收集约束 → 求解约束 → 证伪解 → 裁定
```

每一步都有 FP 出口，不必走完全程就能给出裁定。

### 第 1 步：理解主张

把任意格式描述的漏洞（自然语言、SAST 输出、CVE、JSON、Markdown）解析为一个明确的待验证假设：**什么漏洞、在哪个位置、造成什么影响**。

漏洞描述太模糊无法提取这些信息 → 追问澄清。引用了 CVE 或补丁的，分析 diff 可以加速后续步骤。

### 第 2 步：定位约束系统

1. **核验 Sink**：找到漏洞声称的位置，确认危险操作存在。精确识别注入面——同一函数不同参数的注入风险不同。目标条件须与声称的漏洞类型匹配："数据到达危险函数"不等于特定类型的漏洞，需证明数据能以该类型特有的方式触发。**Sink 不存在、位置错误或代码不可达 → FALSE POSITIVE（错误位置）。**

2. **识别 Source**：不信任漏洞声称的 Source，自己找攻击者可控数据实际从哪进入。按来源类型设定默认立场——外部系统数据默认不可信，数据库数据视为疑似（可能二次注入），文件/环境变量默认不可控（除非有证据证明可被外部控制）。标注可控程度和跨越的信任边界。**没有外部可控的数据源 → FALSE POSITIVE（不可达）。**

3. **追踪 Path**：建立 Source 到 Sink 的数据流，每跳标注变换类型（保持/去除/门控）。路径有分支的（if-else、try-catch、多赋值），每条分支独立分析。**Source 到 Sink 不存在可达路径 → FALSE POSITIVE（不可达）。**

### 第 3 步：收集约束

沿路径逐跳收集三类条件：

- **变换条件**：输入经过此变换后，输出还能触发 Sink 吗？能的话，输入要满足什么？
- **防护条件**：此防护阻断了哪些输入？能绕过吗？门控型查逻辑有没有漏洞，变换型查覆盖的字符集对不对。
- **运行时条件**：攻击者能满足到达此路径的前提吗？

**防护缺失须主动证明**：未发现防护 ≠ 无防护。说"无校验"时，必须主动搜遍整个流程的所有校验点——注解式校验、中间件、辅助方法、上下游调用链，确认确实没有或都已失效。不能只查了一处没发现就认定缺失。

### 第 4 步：求解约束

**从 Sink 反向推导**：Sink 要什么输入 → 逐层反推每步变换之前要什么 → 到达 Source 得到"攻击者必须输入什么"。

反向推导到某一步发现矛盾——无法构造出满足所有条件的输入 → 可能是 FALSE POSITIVE。

约束可满足，构造出具体的 Payload。

### 第 5 步：证伪解

对自己的 Payload 做审查：

- **正向模拟**：Payload 从 Source 开始，逐步走过每个变换，是否真的以触发形态到达 Sink？
- **约束完备性**：遗漏了约束吗？常见遗漏：编码/字符集/截断、框架级防护（中间件/安全头/模板引擎）、运行时前提（鉴权/配置/数据依赖）、隐式路径。
- **假设验证**：构造过程中做了假设吗（如"这个函数是透传的"）？每个假设都要验证。

Payload 未通过证伪 → 修复后重新审查，或降级为 CONDITIONAL / FALSE POSITIVE。

### 第 6 步：裁定

- **TRUE POSITIVE**：Payload 构造成功且通过证伪。先说影响再说技术细节。
- **FALSE POSITIVE**：证明不可能构造出 Payload。说明哪个条件不可满足、在哪一步。
- **CONDITIONAL**：只有特定条件满足时才能构造出 Payload。列出所有条件。
- 附加：置信度（High / Medium / Low，看证据完整性和假设数量）、假设清单（每个假设都是"如果错了裁定会变"的风险点）、证据链摘要。

---

## 校验原则（5 条）

1. **双向质疑**：说"有漏洞"要证明 Payload 可构造，说"无漏洞"要证明防护确实覆盖了所有路径。两个方向都要证据，不靠"看起来危险"或"看起来安全"下结论。
2. **Payload 即证据**：没有可工作的 Payload 就不算 TRUE POSITIVE（逻辑类漏洞需要等价替代证明——例如证明某条业务逻辑可被绕过而不触发预期检查）。
3. **自我对抗**：构造出 Payload 后必须自己试图打破它。自己都证伪不了，这个解才成立。
4. **证据链**：每个结论都能追溯到具体代码位置（file:line + 原文）。不允许逻辑跳跃，推理的每一步都要有代码证据。质疑本身也要有代码事实支撑，不能为了推翻结论而编造不存在的假设或攻击场景。
5. **约束完备性**：证伪的本质是检查有没有漏掉条件。一个解失败，往往不是因为推理错了，而是因为有一层条件没被纳入分析。

---

## 输出结构

```markdown
## 裁定: {TRUE POSITIVE | FALSE POSITIVE | CONDITIONAL}

## 漏洞主张摘要
<规范化的假设>

## 约束系统
### Sink
### Source
### Path

## 约束清单
<逐跳约束>

## Payload / 不可满足证明
<构造的 Payload 或 不可能构造的理由>

## 证伪结果
<正向模拟 + 约束完备性检查结果>

## 裁定依据
<为什么得出此裁定 + 置信度 + 假设清单>
```

## 使用前提

- **输入**：一个漏洞（任意格式描述，单个漏洞）
- **环境**：可访问的源代码（白盒分析）
- **不适用**：无具体漏洞的开放性挖掘、纯黑盒测试、批量验证（每次只处理一个漏洞）
